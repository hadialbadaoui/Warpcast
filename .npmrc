# Use exact version numbers for dependencies
save-exact=true

# Set default registry (public npm registry)
registry=https://registry.npmjs.org/

# Disable package lock for this project (optional)
# package-lock=false

# Require authentication for the default registry (if you use private packages)
always-auth=true

# Configure log level (less verbose)
loglevel=warn